# 💬 ระบบแชทแบบ LINE - คู่มือการใช้งาน

## 📋 สารบัญ

1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [การติดตั้ง](#การติดตั้ง)
3. [การตั้งค่า Database](#การตั้งค่า-database)
4. [การใช้งาน](#การใช้งาน)
5. [ฟีเจอร์หลัก](#ฟีเจอร์หลัก)
6. [API Reference](#api-reference)
7. [Troubleshooting](#troubleshooting)

---

## 🎯 ภาพรวมระบบ

ระบบแชทแบบเรียลไทม์ที่จำลองประสบการณ์การใช้งานแบบ LINE 1-1 สำหรับการสนทนาระหว่างลูกค้าและ Agent/Admin

### ✨ ฟีเจอร์หลัก

- ✅ แชทแบบเรียลไทม์ (WebSocket)
- ✅ ส่งข้อความตัวอักษร
- ✅ ส่งรูปภาพ, วิดีโอ, เสียง, ไฟล์
- ✅ สถานะข้อความ (ส่งแล้ว/อ่านแล้ว)
- ✅ Typing Indicator (กำลังพิมพ์...)
- ✅ AI Assistant แนะนำคำตอบ
- ✅ เปิด/ปิด AI Auto Reply
- ✅ โหลดประวัติย้อนหลัง
- ✅ การแจ้งเตือนแบบเรียลไทม์
- ✅ Responsive Design (Mobile + Desktop)

---

## 🚀 การติดตั้ง

### 1. ติดตั้ง Dependencies

```bash
cd c:\n8n-local\sales-ai
npm install
```

Dependencies ที่จำเป็น:
- `@supabase/supabase-js` - Supabase Client
- `socket.io-client` - WebSocket
- `emoji-picker-react` - Emoji Picker
- `react-dropzone` - File Upload
- `date-fns` - Date Formatting
- `openai` - OpenAI API

### 2. ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local` (ถ้ายังไม่มี):

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_KEY=your_service_key

# OpenAI
OPENAI_API_KEY=sk-...
```

---

## 🗄️ การตั้งค่า Database

### 1. รัน SQL Schema

เปิดไฟล์ `supabase-schema.sql` และรันใน Supabase SQL Editor:

```bash
# ไฟล์อยู่ที่
c:\n8n-local\sales-ai\supabase-schema.sql
```

Schema นี้จะสร้าง:
- ✅ ตาราง `chat_rooms` - ห้องแชท
- ✅ ตาราง `chat_messages` - ข้อความ
- ✅ ตาราง `typing_indicators` - สถานะกำลังพิมพ์
- ✅ ตาราง `ai_suggestions` - คำแนะนำจาก AI
- ✅ ตาราง `notification_settings` - การตั้งค่าการแจ้งเตือน
- ✅ ตาราง `agent_status` - สถานะ Agent
- ✅ Triggers และ Functions
- ✅ Row Level Security (RLS) Policies
- ✅ Realtime Subscriptions

### 2. เปิด Realtime

ใน Supabase Dashboard:
1. ไปที่ **Database** → **Replication**
2. เปิด Realtime สำหรับตาราง:
   - `chat_messages`
   - `typing_indicators`
   - `chat_rooms`
   - `ai_suggestions`

---

## 💻 การใช้งาน

### 1. เริ่มต้น Development Server

```bash
npm run dev
```

### 2. เปิดหน้าแชท

```
http://localhost:3000/chat-v2
```

### 3. โครงสร้างหน้าจอ

```
┌─────────────────────────────────────────────────┐
│  [รายการห้องแชท]  │  [หน้าต่างแชท]           │
│                    │                            │
│  🔍 ค้นหา          │  👤 ชื่อลูกค้า  [🤖 AI]   │
│                    │  ────────────────────────  │
│  📋 ห้อง 1         │                            │
│     ข้อความล่าสุด  │  💬 ข้อความ...            │
│     [AI] [3]       │                            │
│                    │                            │
│  📋 ห้อง 2         │  ┌─────────────────────┐  │
│     ข้อความล่าสุด  │  │ 💡 AI แนะนำคำตอบ:  │  │
│     [Manual]       │  │ "สวัสดีค่ะ..."      │  │
│                    │  │ [ใช้] [ปิด]         │  │
│                    │  └─────────────────────┘  │
│                    │                            │
│                    │  ────────────────────────  │
│                    │  📝 พิมพ์ข้อความ... [ส่ง] │
└─────────────────────────────────────────────────┘
```

---

## 🎨 ฟีเจอร์หลัก

### 1. การส่งข้อความ

#### ข้อความตัวอักษร
- พิมพ์ในช่องข้อความ
- กด Enter เพื่อส่ง (Shift+Enter = ขึ้นบรรทัดใหม่)

#### รูปภาพ
- คลิกปุ่ม 🖼️ หรือลากไฟล์มาวาง
- รองรับ: JPG, PNG, WebP, GIF
- ขนาดสูงสุด: 10MB

#### วิดีโอ
- คลิกปุ่ม 🎥 หรือลากไฟล์มาวาง
- รองรับ: MP4, WebM, MOV
- ขนาดสูงสุด: 50MB

#### เสียง
- กดค้าง 🎤 เพื่ออัดเสียง
- ปล่อยเพื่อส่ง
- หรืออัปโหลดไฟล์เสียง (MP3, M4A, WAV)

#### ไฟล์
- คลิกปุ่ม 📎
- รองรับ: PDF, DOC, DOCX, XLS, XLSX, ZIP
- ขนาดสูงสุด: 20MB

### 2. AI Assistant

#### เปิด/ปิด AI
- คลิกปุ่ม **🤖 AI เปิด** / **🤖 AI ปิด** ที่ header

#### AI แนะนำคำตอบ
เมื่อลูกค้าส่งข้อความมา:
1. AI จะวิเคราะห์และแนะนำคำตอบ
2. แสดงใน panel สีม่วงด้านล่าง
3. คลิก **ใช้คำตอบนี้** เพื่อส่ง
4. หรือคลิก **ปิด** เพื่อพิมพ์เอง

#### AI แบบ RAG (Phase 2)
- ค้นหาคำตอบจาก Knowledge Base
- แสดงแหล่งที่มาของข้อมูล

### 3. สถานะข้อความ

| สัญลักษณ์ | ความหมาย |
|----------|---------|
| ✓ | ส่งแล้ว |
| ✓✓ | ส่งถึงแล้ว |
| ✓✓ (สีน้ำเงิน) | อ่านแล้ว |
| ❌ | ส่งไม่สำเร็จ |

### 4. Typing Indicator

- แสดง "กำลังพิมพ์..." เมื่ออีกฝ่ายกำลังพิมพ์
- หายไปอัตโนมัติเมื่อหยุดพิมพ์ 3 วินาที

### 5. การแจ้งเตือน

- 🔔 เสียงแจ้งเตือนเมื่อมีข้อความใหม่
- 🔴 Badge แสดงจำนวนข้อความที่ยังไม่อ่าน
- 🌐 Web Push Notification (ถ้าเปิดใช้งาน)

---

## 🔧 API Reference

### POST `/api/ai-suggest`

แนะนำคำตอบจาก AI

**Request:**
```json
{
  "message": "ข้อความจากลูกค้า",
  "roomId": "uuid",
  "useRAG": false
}
```

**Response:**
```json
{
  "suggestion": "คำตอบที่แนะนำ",
  "sources": []
}
```

---

## 🎯 Realtime Events

### Subscribe ข้อความใหม่

```typescript
const { messages, sendMessage } = useRealtimeChat({
  roomId: 'room-uuid',
  userId: 'user-id',
  onNewMessage: (message) => {
    console.log('New message:', message)
  },
})
```

### Subscribe Typing Indicator

```typescript
const { typingUsers, sendTypingIndicator } = useRealtimeChat({
  roomId: 'room-uuid',
  userId: 'user-id',
  onTypingChange: (indicator) => {
    console.log('Typing:', indicator)
  },
})
```

---

## 🐛 Troubleshooting

### ปัญหา: ข้อความไม่ขึ้นแบบ Realtime

**วิธีแก้:**
1. ตรวจสอบว่าเปิด Realtime ใน Supabase แล้ว
2. ตรวจสอบ RLS Policies
3. ดู Console สำหรับ errors

### ปัญหา: ไม่สามารถอัปโหลดไฟล์ได้

**วิธีแก้:**
1. ตรวจสอบขนาดไฟล์ (ต้องไม่เกินที่กำหนด)
2. ตรวจสอบ Storage Bucket ใน Supabase
3. ตรวจสอบ CORS settings

### ปัญหา: AI ไม่แนะนำคำตอบ

**วิธีแก้:**
1. ตรวจสอบ `OPENAI_API_KEY` ใน `.env.local`
2. ตรวจสอบว่าเปิด AI ในห้องแชทแล้ว (`is_ai_enabled = true`)
3. ดู Console สำหรับ API errors

### ปัญหา: Typing Indicator ไม่หาย

**วิธีแก้:**
- Typing indicators จะถูกลบอัตโนมัติหลัง 5 วินาที
- ถ้ายังไม่หาย ให้รัน: `SELECT cleanup_old_typing_indicators();`

---

## 📚 เอกสารเพิ่มเติม

- [Supabase Realtime Docs](https://supabase.com/docs/guides/realtime)
- [OpenAI API Docs](https://platform.openai.com/docs)
- [Next.js App Router](https://nextjs.org/docs/app)

---

## 🎉 Phase 2 Features (Coming Soon)

- [ ] Group Chat (แชทกลุ่ม)
- [ ] Voice/Video Call
- [ ] Screen Sharing
- [ ] Message Reactions (👍❤️😂)
- [ ] Reply to Message
- [ ] Forward Message
- [ ] Unsend Message
- [ ] Search Messages
- [ ] Export Chat History
- [ ] Custom Stickers
- [ ] Rich Text Editor
- [ ] Code Syntax Highlighting
- [ ] File Preview (PDF, DOC)
- [ ] Image Editor
- [ ] GIF Support
- [ ] Location Sharing
- [ ] Contact Sharing
- [ ] Poll/Survey
- [ ] Scheduled Messages
- [ ] Auto-Reply Rules
- [ ] Chatbot Builder
- [ ] Analytics Dashboard

---

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อทีมพัฒนา

**สร้างโดย:** Cascade AI Assistant  
**วันที่:** 28 ตุลาคม 2025  
**เวอร์ชัน:** 1.0.0 (MVP)
